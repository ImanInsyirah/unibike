name: appunibike
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Modified this line to be more compatible

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.4
  google_maps_flutter: ^2.2.0
  intl: ^0.19.0
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.2
  provider: ^6.1.2
  firebase_ui_auth: ^1.16.1
  youtube_player_flutter: ^9.1.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  flutter_map_dragmarker: ^7.0.0
  video_player: ^2.9.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Added proper indentation for fonts
  #fonts:
   # - family: League Gothic
    #  fonts:
     #   - asset: assets/fonts/LeagueGothic-Regular.ttf  # Fixed 'assets' to 'asset'